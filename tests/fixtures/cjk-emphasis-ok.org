* CJK Emphasis — Correct Patterns

This file contains properly formatted org-mode markup.
check-org-markup.sh should report OK on this file.

** Emphasis markers with proper spacing

结果： =foo= 错误 ← OK: space between CJK colon and =
代码 =code= 示例 ← OK: spaces on both sides
结果 ~expr~ 。 ← OK: spaces around ~
修正 +strikethrough+ 测试 ← OK: spaces around +
/方案 A/ ：回退 ← OK: italic content tight, / spaced from CJK after closing
*重点* ：内容 ← OK: bold content tight, * spaced from CJK after closing
_强调_ 。结尾 ← OK: underline content tight, _ spaced from CJK after closing

** Math delimiters with proper spacing

对 $x \ge 1$ 测试 ← OK: spaces around $
当 $x > 0$ 时 ← OK: space before CJK after $
由 $\sin(x)$ 定义 ← OK: spaces around $
$x > 0$, 即… ← OK: ASCII comma after $ needs no space

** Correct bold & heading

约 *13x* 提升 ← OK: org bold (single asterisk)
** Heading with space ← OK: heading marker followed by a space

** Completely clear lines

Plain text without any markup delimiters.
中文文本无标记。
